{% extends 'cms/table.html.twig' %}

{% block table_name %}
    Actives
{% endblock %}

{% block table_create_btn %}
    <a class="btn-adm" href="{{ path('app_cms_active_create') }}">Добавить</a>
{% endblock %}

{% block table_content %}
    <table class="table">
        <thead>
        <tr>
            <th>#</th>
            <th>Название</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        {% for active in actives %}
            <tr>
                <td class="col-index">{{ loop.index }}</td>
                <td>{{ active.name }}</td>
                <td class="actions actions-one-line">
                    <a class="btn-adm"
                       href="{{ path('app_cms_active_update', {active: active.id}) }}">Редактировать</a>
                    <a class="btn-adm"
                       href="{{ path('app_cms_active_delete', {active: active.id}) }}">Удалить</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}